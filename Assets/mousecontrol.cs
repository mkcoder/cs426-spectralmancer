using UnityEngine;
using System.Collections;

public class mousecontrol : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Screen screen = gameObject.GetComponent<Screen>();
        Cursor.visible = true;
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
